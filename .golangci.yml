run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

issues:
  exclude-rules:
    - path: main.go
      linters:
        - errcheck
      text: "fmt.Fprintln"
