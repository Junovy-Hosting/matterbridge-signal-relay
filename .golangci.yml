version: "2"
run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

issues:
  exclude-rules:
    # Allow fmt.Fprintln in health handler
    - path: main.go
      linters:
        - errcheck
      text: "fmt.Fprintln"
